# Computer Vision & AI
opencv-python>=4.8.0
numpy<2.0           # CRITICAL: faster-whisper requires numpy<2.0
torch
torchvision
# facenet-pytorch         # Face Rec (Optional)
mediapipe>=0.10.0       # Hand gestures
ultralytics>=8.0.0      # YOLOv8 for Object/Pose

# Audio & Speech
pyttsx3                 # Text-to-Speech (Legacy Offline)
edge-tts                # High-Quality Neural TTS (Online)
SpeechRecognition       # Google Speech API (Fallback)
pyaudio                 # Microphone Input
vosk>=0.3.45            # Offline Speech (Fallback)
faster-whisper          # High-Fidelity Speech (Primary)
openwakeword            # Wake Word Detection
onnxruntime             # Required for openwakeword
webrtcvad-wheels>=2.0.10 # VAD for Windows
scipy>=1.10.0           # Signal processing
# openai-whisper          # (Removed in favor of faster-whisper)

# Dashboard & API
flask                   # Web Dashboard
flask-socketio>=5.3.0   
python-socketio>=5.10.0
requests                
werkzeug                

# Utilities
colorlog>=6.8.0         
pyyaml>=6.0             
psutil>=5.9.0           
python-dotenv           

# Hardware (Raspberry Pi Only - auto-skipped on Windows)
# RPi.GPIO; sys_platform == 'linux'
# spidev; sys_platform == 'linux'

# AI & LLM
google-generativeai     # Gemini
ollama                  # Local LLM Interface
