# Computer Vision & AI
opencv-python>=4.8.0
numpy>=1.24.0      # <2.0 preferred for compatibility, but 2.x accepted with fixes
torch
torchvision
# facenet-pytorch         # Face Rec (Optional)
mediapipe>=0.10.0       # Hand gestures
ultralytics>=8.0.0      # YOLOv8 for Object/Pose

# Audio & Speech
pyttsx3                 # Text-to-Speech (Offline)
SpeechRecognition       # Google Speech API (Fallback)
pyaudio                 # Microphone Input
vosk>=0.3.45            # Offline Speech (Primary)
webrtcvad-wheels>=2.0.10 # VAD for Windows
scipy>=1.10.0           # Signal processing
openai-whisper          # High-Fidelity Fallback

# Dashboard & API
flask                   # Web Dashboard
flask-socketio>=5.3.0   
python-socketio>=5.10.0
requests                
werkzeug                

# Utilities
colorlog>=6.8.0         
pyyaml>=6.0             
psutil>=5.9.0           
python-dotenv           

# AI & LLM
google-generativeai     # Gemini
ollama                  # Local LLM Interface
