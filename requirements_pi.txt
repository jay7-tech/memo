# MEMO - Raspberry Pi Requirements
# Usage: pip install -r requirements_pi.txt

# Core Hardware
RPi.GPIO                # GPIO Control
pyaudio                 # Microphone (Requires: sudo apt install portaudio19-dev)
spidev                  # SPI Interface (Optional)

# Computer Vision & AI
opencv-python-headless>=4.8.0  # Headless (No GUI) for better performance
numpy<2.0               # Use <2.0 for best compatibility with ARM wheels
ultralytics             # YOLOv8 (might be slow, use NCNN/TFLite if possible)
mediapipe-rpi4          # Optimized mediapipe if available, else standard `mediapipe`

# Audio & Speech
vosk>=0.3.45            # Offline Speech (Best for Pi)
sounddevice             # Alternative Audio
pyttsx3                 # Text-to-Speech (Needs espeak: sudo apt install espeak)
SpeechRecognition

# Utilities
psutil
requests
flask
flask-socketio
python-dotenv
colorlog
ollama                  # Use external Ollama server if Pi is too slow
