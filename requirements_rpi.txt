# =============================================
# MEMO - Raspberry Pi 4 Requirements
# =============================================
# 
# These packages are tested on Raspberry Pi 4 (4GB+)
# with Raspberry Pi OS 64-bit (Bookworm)
#
# INSTALL ORDER:
# 1. System packages: sudo apt install -y python3-opencv python3-pyaudio
# 2. PyTorch (special): pip install torch --index-url https://download.pytorch.org/whl/cpu
# 3. This file: pip install -r requirements_rpi.txt
#
# =============================================

# ---------------------------------------------
# CORE - Fast install from piwheels
# ---------------------------------------------
numpy>=1.24.0,<2.0.0          # Use numpy 1.x for compatibility
psutil>=5.9.0                  # System monitoring

# ---------------------------------------------
# COMPUTER VISION - Use pre-built wheels
# ---------------------------------------------
# opencv-python-headless      # SKIP - Install via: sudo apt install python3-opencv
ultralytics>=8.0.0             # YOLOv8 - has ARM wheels

# ---------------------------------------------
# AUDIO & SPEECH
# ---------------------------------------------
pyttsx3>=2.90                  # TTS - pure Python, works on Pi
SpeechRecognition>=3.10.0      # STT - includes Google API
# pyaudio                      # SKIP - Install via: sudo apt install python3-pyaudio
vosk>=0.3.45                   # Offline STT - has ARM wheels!

# ---------------------------------------------
# WEB DASHBOARD
# ---------------------------------------------
flask>=3.0.0                   # Web framework
flask-socketio>=5.3.0          # WebSocket support
python-socketio>=5.10.0        # Socket.IO client
Werkzeug>=3.0.0                # Flask utilities
requests>=2.31.0               # HTTP client

# ---------------------------------------------
# UTILITIES
# ---------------------------------------------
colorlog>=6.8.0                # Colored logging
pyyaml>=6.0                    # Config files

# ---------------------------------------------
# OPTIONAL - May not work on all Pi setups
# ---------------------------------------------
# facenet-pytorch              # SKIP on Pi - needs torch first
# mediapipe                    # SKIP - Limited ARM support
# openwakeword                 # SKIP - May have issues
