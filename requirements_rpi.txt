# =============================================
# MEMO - Raspberry Pi Requirements
# =============================================
# Install with: pip install -r requirements_rpi.txt
# 
# IMPORTANT: Install PyTorch separately FIRST!
# See: INSTALL_RPI.md for full instructions
# =============================================

# Computer Vision (Raspberry Pi compatible)
opencv-python-headless>=4.8.0   # Headless version for Pi
numpy>=1.24.0
# ultralytics - Install from source (see INSTALL_RPI.md)
# torch/torchvision - Install separately (see INSTALL_RPI.md)
# facenet-pytorch - May not work on Pi, use alternative
# mediapipe - ARM support limited, may need alternatives

# Audio & Speech  
pyttsx3                 # Text-to-Speech (Offline)
SpeechRecognition       # Speech-to-Text
# pyaudio - Install via apt: sudo apt install python3-pyaudio
vosk>=0.3.45            # Better offline ASR (has ARM wheels)

# Dashboard & API
flask                   # Web Dashboard
flask-socketio>=5.3.0   # WebSocket for real-time updates
python-socketio>=5.10.0
requests                # HTTP Requests
werkzeug                # Flask utility

# Utilities
colorlog>=6.8.0         # Colored console logging
pyyaml>=6.0             # Configuration management
psutil>=5.9.0           # System stats monitoring

# Raspberry Pi Specific
picamera2               # Pi Camera support (if using Pi Camera)
gpiozero                # GPIO control (optional)
